import { Routes } from '@angular/router';
import { TowSumComponent } from './tow-sum/tow-sum.component';

export const routes: Routes = [

    {
        path:'',
        component:TowSumComponent
    }
];
